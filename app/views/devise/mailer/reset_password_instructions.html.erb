<p><%= @resource.email %>さんお世話になります!</p>

<p>パスワードの変更がリクエストされました。以下のリンクで変更できます。</p>

<p><%= link_to '変更する', edit_password_url(@resource, reset_password_token: @token) %></p>

<p>身に覚えがない場合は、このメールを無視してください</p>
<p>上記のリンクで変更しない限りパスワードが変更されることはありません</p>
